#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int sgDig = 0;

	for (; sgDig <= 0; sgDigit++)
	{
		printf("%d", sgDigit);
	}
	putchar('\n');

	return (0);
}
