#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int sgDig;

	for (sgDig = 0; sgDig <= 10; sgDigit++)
		printf("%d", sgDigit);
	putchar('\n');

	return (0);
}
